# Workspace6 sponsor dashboard

Published page for prospective Workspace6 sponsors. **This repo holds only the built page.**
It is regenerated and pushed automatically by `~/w6-audience/run.sh` every Monday morning.

Do not edit `index.html` here. Edit the source in `~/w6-audience` and let the refresh push it.
